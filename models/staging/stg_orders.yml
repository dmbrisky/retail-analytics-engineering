version: 2

models:
  - name: stg_orders
    description: "Staged Olist order data at one row per order"

    columns:
      - name: order_id
        description: "The primary key for this table."
        data_tests:
          - unique
          - not_null
     