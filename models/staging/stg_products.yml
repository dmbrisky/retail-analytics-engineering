version: 2

models:
  - name: stg_products
    description: "Staged Olist product data at one row per product"

    columns:
      - name: product_id
        description: "The primary key for this table."
        data_tests:
          - unique
          - not_null